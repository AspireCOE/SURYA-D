let number:number;
let word:String;
let boolean:boolean;
let anyType:any;
let array:number[]=[1,2,3];

let any:any[]=[1,"a",true];


const colorRed=0;
const colorGreen=1;
const colorlue=2;

//instead of using above we can use enum

enum color{Red=101, Green=102, Blue=103};
let backgroundColor=color.Blue;
